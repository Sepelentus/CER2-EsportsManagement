import 'package:flutter/material.dart';

class SplashRocket extends StatelessWidget {
  const SplashRocket({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Splash Rocket');
  }
}
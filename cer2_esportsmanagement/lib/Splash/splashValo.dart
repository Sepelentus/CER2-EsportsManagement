import 'package:flutter/material.dart';

class SplashValorant extends StatelessWidget {
  const SplashValorant({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Splash Valorant');
  }
}